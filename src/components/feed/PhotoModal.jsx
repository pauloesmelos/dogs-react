import React from 'react';

const PhotoModal = ({ data }) => {
  React.useEffect(() => {
    console.log(data);
  }, [data]);
  return (
    <div>
        
    </div>
  )
}

export default PhotoModal;